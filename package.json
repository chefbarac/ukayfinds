{
    "scripts": {
        "dev": "vite",
        "build": "vite build && node scripts/generate-sw.mjs",
        "preview": "vite preview"
    },
    "devDependencies": {
        "sass": "^1.99.0",
        "vite": "^6.4.3"
    }
}
